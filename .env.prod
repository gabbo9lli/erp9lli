# .env.prod (Production secrets and variables - KEEP THIS FILE SECURE)
POSTGRES_USER=prod_secure_user
POSTGRES_PASSWORD=VERY_SECURE_PASSWORD
POSTGRES_DB=main_app_db
FLASK_ENV=production
FLASK_COMMAND=gunicorn --bind 0.0.0.0:9000 app:app # Production-ready Gunicorn server